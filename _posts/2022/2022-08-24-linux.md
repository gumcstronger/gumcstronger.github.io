---
layout:     post
title:      "Linux常用配置"
subtitle:   " \"Linux软件及配置\""
date:       2022-09-28 17:15:00
author:     "Gumc"
header-img: "assets/img/2015/post-bg-2015.jpg"
catalog: true
tags:
    - Continuous Updates
---
> 开发经常涉及一些安装和配置。Linux相关的整理了多年都很完善了，这里进行整理下方便记录。

## 安装系统

此处以CentOS Stream 10为例。

* 下载[CentOS Stream](https://www.centos.org/download/)： 含GUI则选择CentOS-Stream-10-latest-x86_64-dvd1.iso
* 使用[Rufus](https://github.com/pbatard/rufus)刻录系统启动盘
* 参考[官方教程](https://infotechys.com/install-centos-stream-10-server-with-gui/)安装CentOS Stream 10

注意：安装时语言选择English，然后进入桌面后再切换语言，然后选择保留旧文件夹，这样可以让文档或下载等目录保持英文，方便shell使用。

## 基础配置

```
# 安装本地rpm：sudo yum localinstall xxx.rpm
# 安装本地AppImage：sudo yum install xxx.AppImage
# 安装库：sudo dnf install xxx

# 启用AppImage, 之后只要右键设置AppImage为可运行就可以双击运行了
sudo dnf install fuse fuse-libs

# 
```

## 远程桌面

### 开启WOL(Wake On LAN)

为了方便远程操作，开启Wake On LAN。[参考资料](https://technotim.live/posts/wake-on-lan/)

* 确保使用有线网卡
* 在BIOS中，确保**Wake on LAN类似的配置时启动的。**
* 使用ethtool开启WOL

  ```bash
  # 列出所有网络端口,查看有线以太网接口,名称通常是 eno1, enp2s0, eth0
  ip a
  # 安装 ethtool
  sudo dnf install ethtool
  # 检查当前的 WOL 状态, 查看 Wake-on: 后面的字母,这个字母一般是 d (disabled)，或者没有 g，你就需要手动启用它
  sudo ethtool <接口名称> | grep "Wake-on"
  # 临时启用 WOL,启动后可以再次查看WOL状态，会发现现在变成g了(这个是必须的，如果不临时开启，永久生效的似乎也开启不了)
  sudo ethtool -s <接口名称> wol g
  # 使 WOL 设置永久生效 
  sudo vim /etc/systemd/system/wol.service
  # 复制以下信息到wol.service
  [Unit]
  Description=Configure Wake-on-LAN for network card
  # Requires=network.target
  # After=network.target

  [Service]
  Type=oneshot
  ExecStart=/usr/sbin/ethtool -s enp2s0 wol g

  [Install]
  WantedBy=multi-user.target

  # 退出编辑器后，让它开机自启
  sudo systemctl enable wol.service
  # 立即启动一次服务
  sudo systemctl start wol.service
  # 查看wol.service的状态，一般是inactive，因为这是一次性的任务，执行后立即结束。如果有错误信息则解决
  systemctl status wol.service

  ```
* 在Windows平台使用[WakeMeOnLAN](https://www.nirsoft.net/utils/wake_on_lan.html)唤醒CentOS

### 开启远程桌面

* RustDesk前置安装

```bash
# 安装xdotool
sudo dnf install xdotool
# 如果提示没有则看看有没有必备的x11组件
rpm -q xorg-x11-server-Xwayland
rpm -q libX11
rpm -q libXext
# 能够看到版本号则说明存在
# Google 搜索 “xdotool rpm centos stream 10 x86_64”,然后下载xdotool-*.x86_64.rpm
# 安装xdotool
sudo dnf localinstall xdotool-3.20211022.1-1.12.x86_64.rpm
```

* 安装RustDesk
```bash
# 安装RustDesk
sudo dnf localinstall rustdesk-1.4.2-0.x86_64.rpm
# 如果还是提示没有libxdo，说明rustDesk 的 RPM 包 spec 文件里写的依赖是 Requires: libxdo (这是一个不带版本号的、模糊的写法) 或者 Requires: xdotool，而不仅仅是 libxdo.so.3
sudo dnf install --nodeps rustdesk-1.4.2-0.x86_64.rpm
# 确定rustdesk.service存在
do systemctl start rustdesk
sudo systemctl status rustdesk
# 如果status有问题则需要解决下
# 
```

* 配置Login自动登录
因为RustDesk使用Wayland需要在登录后才能运行，所以需要配置自动登录。
```bash
# 备份 GDM 配置文件
sudo cp /etc/gdm/custom.conf /etc/gdm/custom.conf.bak
# 编辑 GDM 配置文件
sudo vim /etc/gdm/custom.conf
```
```bash
# 添加AutomaticLoginEnableh和AutomaticLogin两行
# GDM configuration storage
#
# See /usr/share/gdm/gdm.schemas for a list of available options.
[daemon]
# Uncomment the line below to force the login screen to use Xorg
# WaylandEnable=false

# Enable automatic login for a user
AutomaticLoginEnable = true
AutomaticLogin = your_username

[security]

[xdmcp]

[chooser]

[debug]
# Uncomment the line below to turn on debugging
# More verbose logs
# Additionally, Gdm will try to log diagnostic information in case of a crash
#Enable=true
```
然后重启电脑

* 配置分享界面(不用选择)

### 定时关机
```bash
# 创建关机服务
sudo vim /etc/systemd/system/shutdown.service

# 将以下内容复制到shutdown.service中
[Unit]
Description=Scheduled Shutdown Service
DefaultDependencies=no
Before=shutdown.target reboot.target halt.target

[Service]
Type=oneshot
ExecStart=/usr/sbin/shutdown -h now "Scheduled shutdown initiated by systemd timer"

# 创建定时器文件
sudo vim /etc/systemd/system/shutdown.timer

# 添加以下内容到shutdown.timer中
[Unit]
Description=Run shutdown.service daily at 2 AM

[Timer]
OnCalendar=*-*-* 02:00:00
Persistent=true
Unit=shutdown.service

[Install]
WantedBy=timers.target

# 重载 systemd 配置
sudo systemctl daemon-reload

# 启用定时器
sudo systemctl enable shutdown.timer

# 检查您的定时器设置是否正确,下一次运行是什么时候
systemctl list-timers

# 如果需要取消关机
# sudo systemctl stop shutdown.timer
# sudo systemctl disable shutdown.timer
# 如果需要修改关机时间
# 编辑 /etc/systemd/system/shutdown.timer 文件。
# 修改 OnCalendar= 这一行的时间。
# 运行 sudo systemctl daemon-reload。
# 再次用 systemctl list-timers 确认新的执行时间。
```